name: 'demo'
version: '1.0.0'
config-version: 2

profile: 'demo'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target"]

models:
  demo:
    +materialized: view